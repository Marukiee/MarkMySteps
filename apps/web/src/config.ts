/**
 * Instance defaults. Fork-friendly: change these when you self-host under
 * your own domain (also update `server.hostname` in capacitor.config.ts).
 */

/** Used by the Android app when the server field is left empty at login. */
export const DEFAULT_SERVER_URL = 'https://reis.markmaaktmedia.nl';

/** Default "open in Immich" base; overridable per-user in settings. */
export const DEFAULT_IMMICH_PUBLIC_URL = 'https://fotos.markmaaktmedia.nl';
